module.exports = (description, reviews, bookings, neighborhood) => `
  <div id="description-service">${description}<div>
  <div id="reviews-service">${reviews}<div>
  <div id="bookings-service">${bookings}<div>
  <div id="neighborhood">${neighborhood}<div>
`;
